using TestChecker.Core;
using TestChecker.Core.Enums;

namespace WebApplicationChildClient
{
    public class ChildClient : TestCheckable
    {
        public ChildClient(string baseUrl) : base(baseUrl)
        {
        }
    }
}